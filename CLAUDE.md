# Project Context & AI Working Memory

## 1. Project Mission & Overview
- **Project Name**: gl-pipe
- **Mission**: A single-binary, keyboard-first TUI for managing, searching, inspecting, and batch-triggering GitLab CI/CD pipelines across multiple repositories at once, against both GitLab.com and self-hosted instances.

### Overview
Triggering, inspecting, retrying, and cancelling pipelines across many repos today means many browser tabs. gl-pipe collapses that into one Helix-style terminal interface: multi-select repos in a fuzzy-filterable explorer, fire a parameterized pipeline at all of them at once, then watch the resulting multi-project pipeline/job matrix and stream logs — all from the keyboard.

A real user's day-to-day is more MR-centric than project-centric: "here's a pile of MRs with failed pipelines, retry them" — without necessarily knowing which repo each one lives in. `<Space> m` ("my MRs") and `M` (a project's MRs) both jump straight into the *same* pipeline matrix everything else feeds, so sort/filter/bulk-retry apply uniformly regardless of whether the pipelines came from staged projects, a ref search, or an MR selection.

---

## 2. Tech Stack & Run Commands
- **Language & Runtime**: Go 1.26 (module requires 1.23+), no CGO anywhere in the build
- **Frameworks & Libraries**: `charmbracelet/bubbletea` v1.3.10, `bubbles` v1.0.0, `lipgloss` v1.1.0, `gitlab.com/gitlab-org/api/client-go` v1.46.0, `alecthomas/kong`, `gopkg.in/yaml.v3`, `sahilm/fuzzy`, `golang.org/x/mod/semver`
- **Testing Framework**: standard `testing` package + `httptest` for the API layer

### Quick Commands
- **Install Dependencies**: `go mod download`
- **Run Dev Build**: `go run ./cmd/gl-pipe`
- **Run All Tests**: `go test ./...`
- **Run With Race Detector**: `go test ./... -race` (requires a C toolchain — CGO_ENABLED=1 + gcc/clang; **not available in the sandbox this was built in**, so the race detector has not actually been run against this code yet)
- **Run Single Test**: `go test ./internal/ui/components/ -run TestVariables_DispatchEmitsProjectsRefAndVars -v`
- **Lint/Format**: `gofmt -l .` (should print nothing), `go vet ./...`
- **Build**: `go build -o gl-pipe.exe ./cmd/gl-pipe` (Windows) / `go build -o gl-pipe ./cmd/gl-pipe` (Unix)

### Local Config
First run writes `%APPDATA%\gl-pipe\config.yaml` (Windows) or `~/.config/gl-pipe/config.yaml` (Unix). Per-instance project caches live alongside it as `cache-<instance>.json`. Use `--config <path>` to point at an alternate file for testing without touching the real one.

---

## 3. Architecture & Invariants
- **Pattern**: Elm-architecture TUI (Bubbletea) over a thin GitLab API wrapper, feature-organized under `internal/`.
- **Rules & Invariants**:
   1. No goroutine ever touches model state directly. Every GitLab call is a `tea.Cmd` closure returning a `tea.Msg`; all mutation happens in `Model.Update`. This is the entire race-freedom story — see `internal/ui/commands.go`.
   2. Async responses carry a monotonic `reqID`. `Update` drops any message whose `reqID` is older than the pane's current generation (`genProjects`, `genPipelines`, `genJobs`, `genLogs` in `internal/ui/model.go`), so an abandoned request can't clobber fresher state. Tested in `internal/ui/model_test.go`.
   3. Log streaming is a channel + self-rescheduling `tea.Cmd`: `startLogStreamCmd` launches a producer goroutine (`api.Client.StreamJobTrace`) writing to a channel, and `waitForLogChunkCmd` blocks on one receive per call, re-issuing itself until the job reaches a terminal status or the request is canceled. GitLab's trace endpoint has no incremental API — each poll returns the full trace, and the diff-to-suffix happens client-side in `StreamJobTrace`.
   4. Key dispatch precedence is strict: active modal (`variables` / `settings` / `presets` / `logViewer`) → leader menu → focused pane → global. `<Space>` only opens the leader menu when the focused pane reports no text input focused (`HasTextFocus()` on `ProjectList`, `Variables`, `LogViewer`). See `internal/ui/dispatch.go`.
   5. `internal/api` returns domain structs (`api.Project`, `api.Pipeline`, `api.Job`, ...) — never `gitlab.*` SDK types — so the UI layer stays decoupled and the API layer is unit-testable against `httptest` without touching Bubbletea.
   6. Business logic lives in `internal/`; `cmd/gl-pipe/main.go` only parses flags and starts the program.
   7. The project explorer only ever syncs from an instance's configured `default_groups` — there is no "list every project on the instance" path.

### Known simplifications (intentional, not oversights)
- `gx` as an alternate open-in-browser chord was dropped: bubbletea delivers `g` and `x` as two independent `KeyMsg`s, and `g` is already bound to "jump to top" per the nav spec, so a chord would need pending-prefix state machinery not otherwise justified by scope. `<Space> o` is the only bound path to open-in-browser.
- Batch pipeline trigger uses one shared `ref` field in the trigger modal, overridden per-project only when that project has a `T`-locked tag (`ProjectList.LockedRef`). There's no per-project ref editing inside the trigger modal itself.
- Retry counts in the job matrix are computed client-side (position within same stage+name, since GitLab's job list API doesn't expose a retry count field directly) — see `api.Client.ListJobs`.
- Settings (`<Space> s`) supports switching instances; it does not yet support editing presets or TTL in-place (both are read-only display there — edit `config.yaml` directly).

---

## 4. Active Backlog

| Status | Effort | # | Title | Description |
|--------|--------|---|-------|-------------|
| [DONE] | [XL] | 001 | **Project scaffold, config, cache, API layer** | Go module, YAML config with token indirection, JSON project cache with TTL/fuzzy ranking, client-go wrapper with domain types. Full `httptest` coverage. |
| [DONE] | [XL] | 002 | **Bubbletea UI: wizard, explorer, pipeline/job matrix, log viewer, leader menu, settings** | All views wired end-to-end against the API layer with generation-counted async dispatch. |
| [DONE] | [S]  | 003 | **README + keybinding table + cross-compile docs** | — |
| [TODO] | [M] | 004 | **Live-instance smoke test** | Every flow was verified by unit test and by launching the built binary (wizard renders, explorer renders against a stubbed config), but never driven end-to-end against a real GitLab instance — no PAT was available in the build environment. Do this before relying on it for real triggers. |
| [TODO] | [S] | 005 | **Run `go test ./... -race`** | The sandbox this was built in has no C toolchain (`CGO_ENABLED=1` requires gcc/clang), so `-race` has never actually executed against invariant #1. Run it on a machine with a C toolchain before trusting the race-freedom claim beyond code review. |
| [TODO] | [S] | 006 | **Settings: in-place preset/TTL editing** | Currently read-only in the `<Space> s` screen; editing requires hand-editing `config.yaml`. |
| [TODO] | [XS] | 007 | **textinput cursor blink** | `Model.Init()` doesn't return a blink `tea.Cmd` for the wizard/filter/search inputs — cursors are static rather than blinking. Cosmetic only. |
| [DONE] | [S] | 008 | **Fix: empty/unconfigured project sync silently cached as "fresh"** | Real bug hit by a user on first real-world use: syncing with zero `default_groups` was cached with `SyncedAt=now`, so the next launch saw a "fresh" empty cache and showed nothing — no projects, no error, no status. `Init()` now also resyncs whenever the cached project list is empty regardless of TTL, and the "not configured" case gets its own explicit status message and is never cached as fresh. Self-heals existing poisoned caches with no manual cleanup. |
| [DONE] | [M] | 009 | **Group discovery picker (`<Space> g`)** | Directly requested by a user who got stuck hand-typing `default_groups` into `config.yaml` after already finishing the wizard. Fetches every group the token can see (`api.Client.ListGroups`), lets the user multi-select via the same table/filter/checkbox pattern as the rest of the app, and merges the picks additively into the active instance's `default_groups` before triggering a resync. |
| [DONE] | [M] | 010 | **Fix: Enter on a project did nothing** | Two bugs: (a) Enter was never wired to any action in the explorer pane; (b) the real root cause — `bubbles/table` v1.0.0's `SetRows` only clamps the cursor downward, so any table that starts empty (cursor forced to -1) and later gets its first real rows stays permanently un-selectable. Added `components.setRows()` (re-clamps via `SetCursor` after every `SetRows`) at all 5 table call sites. Also fixed `pipelinesLoadedMsg` to replace the matrix instead of accumulating into leftovers from a prior project/batch trigger. |
| [DONE] | [S] | 011 | **Pipeline matrix: Date column + sortable by any field** | User request. Added a `Date` column (relative age, e.g. "5m ago"), default sort by Date descending, `s` to cycle sort field (Date/Project/Status/Ref/SHA/Author/Duration), `S` to reverse direction without changing field. Originally applied to `pipelines` directly; superseded by 015 below, which introduced a separate `filtered` view that sort now operates on. |
| [DONE] | [M] | 012 | **View pipelines across multiple staged projects at once** | User request: reuses the `x`-staging convention already used by the trigger modal (`SelectedProjects()` — staged, or highlighted fallback). Enter now clears the matrix once, then issues one `pipelinesForProjectCmd` per selected project in a `tea.Batch`; `pipelinesLoadedMsg` merges each project's results in via `AddOrUpdate` as it arrives, so the matrix fills incrementally rather than waiting on the slowest project. Combined with sortable-by-any-field (011), this is the "Multi-Project Pipeline Matrix" from the original spec. |
| [DONE] | [M] | 013 | **View job matrices across multiple staged pipelines at once** | Direct follow-up user request, same pattern one level down: `PipelineList` now tracks `Pipelines []api.Pipeline` (was a single `Pipeline` field) and `jobs []api.Job` shared across all of them; `ClearJobs`/`AddJobs` (insert-or-update by job ID) replace the old single-pipeline `SetJobs`. Enter on staged pipelines clears once and batches `jobsForPipelineCmd` per pipeline, same as the project-level flow. Job matrix gained `Project` and `Pipeline` (`#IID`) columns so mixed-pipeline rows stay distinguishable. `jobActionMsg` (retry/cancel) now carries `pipelineID` so its post-action refresh targets the right pipeline without disturbing the others in view. |
| [DONE] | [M] | 014 | **Fix: staged count leaked / never returned to zero** | Real bug: `Selected[id] = !Selected[id]` on a `map[K]bool` sets the entry to `false` on unstage rather than removing the key, so `len(Selected)` — used both for the displayed "N staged" count and every `len(Selected) == 0` "is anything staged?" check — only ever grew. This silently broke the "staged, or highlighted fallback" convention everywhere it's used: fully unstaging a project or pipeline left the fallback never engaging (stale-but-false entries kept the map non-empty), so e.g. re-pressing Enter after unstaging everything did nothing instead of falling back to the highlighted row — reported as "unstaging a project doesn't remove its pipelines from the viewer." Fixed with a shared generic `toggleSet[K](set map[K]bool, key K)` helper (deletes on off) applied at all 5 toggle call sites (project list, pipeline matrix, job matrix, group picker ×2). |
| [DONE] | [M] | 015 | **Pipeline matrix: filter by ref/status/project/author/SHA** | User request: find every pipeline for a branch shared by several MRs, and narrow the matrix by status. Added a `/`-filter (own `filtered []api.Pipeline` view, mirroring the project explorer's `all`/`filtered` split) that substring-matches (case-insensitive) across ref, status, project name, author, and SHA in one box — covers both asks without a separate status-only mode. Sort now operates on `filtered`, not the underlying `pipelines`; staging (`Selected`) intentionally still iterates the full `pipelines` set so staged rows survive a filter that hides them. `SetPipelines` (start of a new Enter batch) resets the filter so stale filter text can't hide freshly-loaded rows. |
| [DONE] | [M] | 016 | **Search pipelines by ref across every project, no staging required (`<Space> b`)** | Direct follow-up: the `/` filter (015) only searches what's already loaded, which still requires knowing/staging the right project first. `api.Client.ListPipelinesByRef` uses GitLab's server-side `ref` filter (exact match, not substring) on `ListProjectPipelinesOptions`. `<Space> b` opens a text modal (`ui.refSearch`); on submit, `searchPipelinesByRef` fires one `pipelinesByRefCmd` per project in `cacheIdx.Projects` — the *whole* synced set, not staged/highlighted — reusing the same `pipelinesLoadedMsg` handling as viewing staged projects' pipelines. Also added batch-completion tracking (`Model.pipelinesPending`/`pipelinesErrored`) shared by both flows via a new `startPipelinesBatch` helper: the status line now reports one summary once every response is in, instead of flickering per-response — necessary once a "search" can span dozens of mostly-empty-result projects. |
| [DONE] | [M] | 017 | **Fix: table columns were fixed-width, truncating repo/job/MR names regardless of terminal size** | Real usability bug reported directly ("repos are cut off"). Every table declared its widest column as a hard constant, correct at one width and wrong at every other. Added `flexColumnWidth`/`splitFlexWidth` (`components/tablehelpers.go`), which every table's `SetSize` now uses to recompute flexible column(s) from the real terminal width, accounting for `bubbles/table`'s per-column padding overhead, with a floor so a pathologically narrow terminal degrades instead of panicking. |
| [DONE] | [XL] | 018 | **Merge requests as a first-class resource (`<Space> m`, `M`)** | User pushback: most of their actual GitLab interaction is MR-centric, and pipelines-only wasn't the "seamless" workflow they wanted — specifically, retrying a pile of interrelated failed pipelines discovered via MRs. Rather than a new pipeline UI, MRs jump into the *existing* pipeline matrix (`Model.startPipelinesBatch`, generalized from `(projects, fetch)` to `(n int, build func(id reqID) []tea.Cmd)` so pipeline-list, ref-search, and MR-pipeline batches all share it) — so sort/filter/bulk-retry all apply automatically to MR-derived pipelines with zero new code in `PipelineList`. New: `api.MergeRequest` domain type, `ListMyMergeRequests` (global endpoint, `assigned_to_me` + `created_by_me` scopes merged/deduped, no per-project iteration needed — unlike the ref search, MRs have a real cross-project list endpoint), `ListProjectMergeRequests`, `ListMergeRequestPipelines`; `components.MRList` modal (fuzzy filter, `x`-stage, `Enter` uses the same "staged, or highlighted fallback" convention as `ProjectList`/`PipelineList` rather than `GroupPicker`'s separate save-row, since this is an "act on what I'm looking at" flow, not a "configure and save" one). `<Space> m` = every synced project (single global request); `M` on the explorer = staged/highlighted project(s) specifically (batched per project, same `mrsPending`/`mrsErrored` completion-summary pattern as the pipeline batches). |
| [DONE] | [L] | 019 | **k9s-inspired visual redesign** | User named k9s directly as the reference. Discovered a hard constraint first: `bubbles/table` v1.0.0 measures cell width with `mattn/go-runewidth`, which has no concept of ANSI escapes — coloring individual cell *text* would make it count escape-code bytes as visible characters and truncate mid-sequence, corrupting the row (locked in by `TestTableStyles_CellHasNoColorAttribute`). So: color lives in the header (safe — applied after the plain title is already truncated) and the selected-row cursor band (safe — `Selected` wraps the whole already-rendered row, but only works cleanly if `Cell` itself injects no color/reset of its own), never inside a data cell; status is distinguished with a plain-text icon (`components.StatusIcon`) instead of color for that reason — `ui.StatusBadge` (the old colored, table-unsafe version) was dead code and got removed. Cyan (`51`) accent palette across the board; `components.TableStyles()` (bold accent header, solid cyan cursor band) wired into all 6 tables; column titles uppercased; `components.RenderHelp` gives every component's bottom hotkey line the same "bold accent key, muted description" treatment (previously plain, and job-matrix/explorer had no help line at all); root `View()` gained a two-line k9s-style header (title badge + instance, then a breadcrumb naming the current view) and the explorer/pipeline panes are now wrapped in `contentBorderStyle` (they weren't bordered at all before — `focusedBorderStyle`/`blurredBorderStyle` existed in `styles.go` but were dead code, never applied). `internal/ui/components/style.go` duplicates (doesn't share — avoids the cycle) the relevant slice of `internal/ui`'s palette, since `components` can't import `ui`. |
| [DONE] | [M] | 020 | **Fix: blob search did nothing at all** | Real bug, found by the user pressing enter and getting no result and no error. Root cause: `components.BlobSearchRequestMsg` was emitted by `ProjectList` on submit, but the root model's `Update` had no case for it whatsoever — the message was constructed, returned as a `tea.Cmd`, dispatched into `Update`'s big type switch, matched nothing, and was silently discarded. `blobSearchCmd` and `blobSearchResultsMsg` existed and were wired to each other, just never triggered. Added the missing `case components.BlobSearchRequestMsg:` and gave it (and the results handler) a dedicated `genBlob` generation counter — it had been piggybacking on `genProjects`, which a concurrent project sync could bump and silently swallow the results of. Second, unrelated bug in the same feature: the query box was a single `"group: query"` string split on the first `:`, which broke the moment the query itself used GitLab's own `path:`/`filename:`/`extension:` search qualifiers (also `:`-delimited) — `path:src/main extension:java` would misread `path` as the group name. Replaced with two separate fields (group, query — `ProjectList.groupInput`/`queryInput`, `Tab` to switch), removing the ambiguity entirely; the query now passes through to GitLab unmodified. Documented in README that those qualifiers are Advanced-Search-only (Elasticsearch) and what they actually support (`path:` is substring match, not a glob — `src/main/**/*.java` is not valid syntax). |
| [DONE] | [M] | 021 | **Stage/unstage all (`a`)** | Direct user request, added across every staged list for consistency: project explorer, pipeline matrix, job matrix, MR list, and the group picker. Shared generic `toggleSetAll[K](set map[K]bool, ids []K)` (`selectionset.go`) mirrors `toggleSet`'s delete-on-off discipline and toggles between "stage all" and "unstage all" based on whether every given id is already staged. Deliberately scoped to whatever's currently *visible* (`p.filtered`/`l.filtered`/`g.filtered`) rather than the full underlying set, so it respects an active `/` filter — the job matrix has no filter of its own, so there `a` covers the full loaded job list. Found and fixed a second, independent instance of the pre-`toggleSet` leaking-count bug while wiring this in: `GroupPicker`'s `enter`-on-a-row handler still did `Selected[k] = !Selected[k]` directly (only the `x` handler had been fixed previously). |
| [DONE] | [L] | 022 | **Fix: `T` (lock latest tag) never visibly did anything; made it batch/toggle-aware; added a ref browser to the trigger modal** | Three asks in one message. (1) Real bug: `refsLoadedMsg`'s handler mutated `ProjectList.LockedRef` (an exported map field) directly from the root model, which updated the data but never called `syncRows()` — the table's cached row strings never rebuilt, so the Ref column silently never changed on screen even though the lock had genuinely taken and a status message *had* fired (easy to miss). Added `ProjectList.SetLockedRef`/`ClearLockedRef`, which mutate and re-sync together; there is now no code path that can touch `LockedRef` without the UI reflecting it. (2) `T` now uses `SelectedProjects()` (staged, or highlighted fallback — the same convention as every other batch action) instead of only ever acting on the highlighted row, and toggles: if every targeted project is already locked, `T` unlocks them all instead of re-fetching. Combined with `/` filter + `a` (select all), this gives "lock every project matching a filter" for free with no new UI. Batch fetches share the established `genRefs`/`refsPending`/`refsErrored` completion-summary pattern, plus `refsSkipped` (valid response, just no semver tags — distinct from an API error) for an accurate final status line. (3) Separate ask: the trigger modal's ref field was a blind text box. Added `Ctrl+r` to browse branches+tags (`api.Client.ListBranches` + `ListTags` merged via a new `loadAllRefsCmd`) and pick one, fetched for the *first* staged project only (the trigger modal already shares one ref field across a whole batch — a documented, pre-existing simplification, not new scope creep). Uses its own `genRefPicker` generation counter, separate from `genRefs`, since the two ref-fetching flows (explorer's lock-to-tag vs. trigger-modal's browse-and-pick) can be in flight independently. |
| [DONE] | [M] | 023 | **Fix: `T` couldn't unlock a partially-locked batch; added a second "lock to latest" strategy (`t` = latest created tag)** | User report: "I had 33 projects staged, locking to the latest ref worked for all but one, which remained on default, but pressing T again didn't unlock them all" — plus a request for a second definition of "latest" beyond SemVer. Root cause of the unlock bug: 022's toggle used an `allLocked` condition, so any single un-lockable project (no qualifying tag) in an otherwise-locked batch permanently blocked the whole batch from ever unlocking via `T` again. `toggleLockLatestTag()` renamed to `toggleLockRef(strategy refLockStrategy)` (`dispatch.go`) with the condition changed to `anyLocked`: if anything in the targeted set is currently locked, unlock every currently-locked project in it (untouched ones stay untouched) instead of re-fetching. Regression-tested directly against the reported scenario (`TestHandleKey_CapitalTUnlocksOnlyTheLockedOnesInAPartialBatch`). Second ask: added `refLockStrategy` enum (`refLockSemVer` / `refLockLatestCreated`) with `.pick()`/`.label()` methods and a `Model.refLockMode` field (`model.go`); `T` keeps its existing SemVer behavior, new key `t` picks by tag creation date instead (`api.LatestCreatedTag`, sibling to the existing `api.LatestSemVerTag`) — for repos that don't tag with version numbers. Required adding `api.Ref.CreatedAt time.Time`, populated in `ListBranches` (from the tip commit) and `ListTags` (from the tag's own timestamp) via the existing `timeOrZero` helper; verified against the vendored client-go source that `Tag.CreatedAt`/`Commit.CreatedAt` are real fields before building on them. |
| [DONE] | [M] | 024 | **Pipeline/job matrix: automatic polling + manual refresh (`r`)** | User asked directly: "Does the pipeline view / job view poll? If not can we add a button to poll (or poll automatically whatever is easiest)." Neither matrix updated after its initial load — a running pipeline just sat there until you left and re-triggered `Enter`. Added `api.PipelineStatus.Terminal()` (success/failed/canceled/skipped/manual — manual counts as terminal since it won't move without a person clicking play). Repurposed the pre-existing but never-wired `tickMsg`/`Init()` scaffolding into a single self-rescheduling `pollTickCmd` (`commands.go`, 10s interval) started once in `Init()` and kept alive for the life of the program via `Model.pollTick` (`model.go`) — it reschedules itself unconditionally so the loop never dies, but only actually re-fetches when `Model.shouldPoll()` is true (on the pipelines pane, nothing already loading, and `PipelineList.NeedsPoll()` reports something non-terminal still showing). Refresh itself (`Model.refreshActiveMatrix`) re-fetches by individual item rather than re-running whatever query originally populated the matrix (staged projects / ref search / MR selection all converge here) — `pipelineDetailCmd` per pipeline in pipeline mode, `jobsForPipelineCmd` per pipeline in job mode — reusing existing single-item fetch commands that were already wired to patch-in-place handlers. Same helper backs a manual `r` key (new `components.RefreshRequestMsg`) in both matrix modes for an on-demand refresh independent of the poll cadence. |
| [DONE] | [S] | 025 | **Project explorer filter: depth anchor + exclude tokens** | User request, framed as an exploratory question: they wanted "all the services in a group, not its subgroups" without hand-picking each project, since `/` was pure `sahilm/fuzzy` subsequence matching against `PathWithNamespace` with no notion of path depth — `backend/` still matched `backend/subteam/svc` because "these characters appear in order somewhere" doesn't distinguish a group from its subgroups. `cache.Index.Filter` (the only caller is `ProjectList`) now tokenizes the query on whitespace (AND across tokens) with two special prefixes borrowed from fzf's extended-search syntax: a token ending in `/` (e.g. `backend/`) is a hard, non-fuzzy anchor — case-insensitive prefix match with no further `/` after it, i.e. direct children only; a token starting with `!` excludes any path containing it. Plain tokens keep the original fuzzy-ranked behavior, now AND'd across multiple terms via `fuzzyRankAND` (summed per-term score) instead of concatenating terms into one broken subsequence pattern. The two patterns compose in one query (`backend/ !legacy`), which was the actual follow-up ask ("is it possible to do both"). Filter placeholder and README updated with the syntax table. |
| [DONE] | [M] | 026 | **Project explorer: per-project ref override with a ref browser (`Ctrl+r`)** | Direct follow-up user request: `T`/`t` lock a whole batch to "latest" by one of two definitions, but sometimes a few projects in that batch need an arbitrary ref instead (a hotfix branch, a specific older tag) — "I might want to lock them all to the latest semver but override a few... would also be good to see the available refs to override." Extracted the trigger modal's existing branch/tag browser overlay (previously private fields `refPickerActive`/`refOptions`/`refCursor` + `updateRefPicker`/`refPickerViewString` embedded directly in `Variables`) into a shared `components.refPicker` type (`refpicker.go`: `Open`/`update`/`viewString`), since the explorer needed the exact same "list, move, pick, esc" overlay for a second, unrelated purpose — `Variables` was refactored onto it with no behavior change (existing tests updated only for the field rename). `ProjectList` gained a `modeRefPicker` mode plus `PrepareRefOverride(projectIDs)` (records the target(s) ahead of the async fetch) and `OpenRefOverridePicker(refs)`; selecting a ref calls the existing `SetLockedRef` for every recorded target, reusing the same `LockedRef` map `T`/`t`/dispatch already read from — no new plumbing needed on the trigger-dispatch side. `HasTextFocus` simplified from an enumerated mode list to `mode != modeBrowse`, so any future overlay is covered automatically. New `Ctrl+r` binding on the explorer (`dispatch.go`'s `openExplorerRefPicker`); since the trigger modal's `Ctrl+r` and the explorer's `Ctrl+r` both funnel through the same `loadAllRefsCmd`/`refPickerLoadedMsg`, added `Model.refPickerFor` (`refPickerForTrigger`/`refPickerForExplorer`) set at fetch time so the response opens the right component's overlay. Initial version targeted `SelectedProjects()` (staged, or highlighted fallback) like every other batch action — user immediately clarified the actual workflow: stage a batch, lock it to SemVer via `T`, *then* highlight individual projects to override just those, without disturbing the rest of the staged batch. `openExplorerRefPicker` was corrected to always target `Highlighted()` and ignore staging entirely — the one explorer action that deliberately doesn't follow the "staged, or highlighted" convention, and documented as such at both the code and README level so it doesn't read as an inconsistency later. |
| [DONE] | [S] | 027 | **Fix: ref picker overlay couldn't scroll past its first screenful** | User report: browsing refs for a project with quite a lot of them, "the element goes off the screen and I can't scroll up or down, i just see a fixed subset (the earliest ones I think)." Root cause: `refPicker` (introduced in 026) rendered its whole `options` slice as a flat hand-built string every frame, with a manual `cursor int` moved by `j`/`k` — nothing capped how many lines got printed or scrolled the visible window to follow the cursor once it moved past what the terminal could show, unlike every other list in the app (`ProjectList`/`PipelineList`/`MRList`/`GroupPicker`), which all use `bubbles/table` and get windowed scrolling for free. Rewrote `refPicker` to wrap a `table.Model` (fixed `WithHeight(15)`, same as `GroupPicker`'s un-resized modal pattern) instead of the manual cursor+string: movement keys now fall through to the table's own `Update` (its default keymap already binds `j`/`k`/`up`/`down`/pgup/pgdown/`g`/`G`), which is what actually gives scrolling — `esc`/`enter` are still intercepted directly, same shape as `GroupPicker.Update`. No changes needed in `Variables` or `ProjectList`, since both already went through `refPicker`'s `Open`/`update`/`viewString` and knew nothing about its internals. Added a regression test selecting a ref past the fixed 15-row window (proving cursor↔options indexing survives scrolling) and a bounded-output check for a 50-ref list. |

---

## 5. Decision Log (ADRs)

| Date | Title | Context | Decision | Rationale |
|------|-------|---------|----------|-----------|
| 2026-08-24 | Use `gitlab.com/gitlab-org/api/client-go` instead of `xanzy/go-gitlab` | The original spec named `xanzy/go-gitlab`, which is archived and frozen at v0.115.0 (Dec 2024) | Use GitLab's official successor module, v1.46.0 | Actively maintained, near-identical API surface, current endpoint coverage (blob search, pipeline variables, job traces) |
| 2026-08-24 | Flat JSON project cache instead of SQLite | Spec allowed either | Plain JSON file loaded into memory, fuzzy-filtered in-process via `sahilm/fuzzy` | Zero extra deps, no CGO, instant for realistic project-list sizes (tens of thousands); `modernc.org/sqlite` would add real binary bloat for no benefit at this scale |
| 2026-08-24 | Token indirection beyond plaintext YAML | Spec's config schema stores PATs in plaintext | Support `token: literal`, `token: ${ENV_VAR}`, and `token_command: <cmd>` (highest precedence); config written 0600 | Plaintext-only was the spec default but a PAT sitting readable on disk was worth avoiding cheaply; OS keyring was considered but rejected as an added dependency + cross-platform failure surface not justified for a first pass |
| 2026-08-24 | Bubbletea v1.3.10, not v2 | bubbletea v2.0.9 exists on the proxy | Pin to the v1 line | `bubbles` v1.0.0 and the wider component ecosystem target bubbletea v1; v2 is new enough that component compatibility wasn't worth the risk for this build |
| 2026-08-24 | `go.mod`'s `go` directive lowered from 1.26.7 to 1.25.0 | `go mod init` recorded the exact toolchain patch version of the build sandbox as a hard minimum, breaking the build for a user on go1.26.5 (a different, older-but-compatible install) | Lowered to 1.25.0, the true floor per every direct dependency's own `go.mod` (`golang.org/x/mod` v0.40.0 needs 1.25.0; everything else needs less) | `go mod init`/`go get` write whatever toolchain is on the machine that ran them, not the actual language-feature floor — worth checking explicitly rather than trusting the auto-written value |
| 2026-08-24 | Group discovery picker merges additively, not by replacement | User confirmed via direct question when this was built | Selecting groups in `<Space> g` adds to `default_groups`, never drops entries not re-selected | Running discovery again to add one more group shouldn't risk silently losing earlier picks |
